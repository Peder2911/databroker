
import setuptools

setuptools.setup(
        name = "databroker",
        version="0.0.1",
        author="Peder G. Landsverk",
        packages = setuptools.find_packages(),
        install_requires=[
            ])
